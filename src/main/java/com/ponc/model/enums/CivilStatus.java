package com.ponc.model.enums;

public enum CivilStatus {
    CASADO,      // Casado(a)
    SOLTERO,     // Soltero(a)
    VIUDO,       // Viudo(a)
    DIVORCIADO,  // Divorciado(a)
    CONVIVIENTE  // Conviviente / union de hecho
}
